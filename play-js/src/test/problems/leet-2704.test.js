import { describe, test } from "bun:test";
import expect from "../../problems/leet-2704";

describe('2704', () => {
    test('case1', () => {
        expect(5).toBe(5)
    })

    test('case2', () => {
        expect(5).notToBe(null)
    })
})
